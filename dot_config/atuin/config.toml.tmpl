{{- if .is_work }}
# TODO: configure local atuin sync server for work machines
# sync_address = "https://atuin.example.com"
{{- else }}
sync_address = "https://api.atuin.sh"
{{- end }}
auto_sync = true
sync_frequency = "5m"
search_mode = "fuzzy"
filter_mode = "global"
style = "full"
inline_height = 0
enter_accept = false
update_check = false
secrets_filter = true
